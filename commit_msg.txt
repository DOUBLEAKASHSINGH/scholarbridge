Fix Profile Syncing, Add Searchable Country Dropdown, & Resolve 404s

- Upgraded `AuthContext.tsx` to use Firebase `onSnapshot` instead of `getDoc`, ensuring the local user state syncs with the database in real-time without needing a manual refresh.
- Built a custom `SearchableSelect` pure-Tailwind component for the "Country of Residence" field to easily filter through a list of countries.
- Added all missing editable fields (Full Name, Country, Field of Study) to the Dashboard Profile page.
- Fixed sidebar 404 errors by building the `/dashboard/settings` and `/dashboard/opportunities` pages, completely migrating Admin functionality natively into the Dashboard structure.
