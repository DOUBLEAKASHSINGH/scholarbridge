UI Polish: Improved Input Text Visibility

- Globally updated all form inputs, textareas, and select elements across the application (Login, Signup, Onboarding, Admin pages).
- Enforced `text-slate-900` to ensure user-typed text is clearly visible and dark, fixing the issue where text blended into the input backgrounds.
- Added `placeholder:text-slate-400` so placeholder text remains distinct from the user's typed input.
