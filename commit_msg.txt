Phase 4: AI Matching Server Action implemented

Breakdown of Progress:
Step 5: AI Matching Engine (The Core Feature)
- Created the Next.js Server Action in /app/actions/match.ts.
- Used the OpenAI SDK (gpt-4o) with response_format: { type: "json_object" } to enforce structured data.
- Engineered a system prompt instructing the AI to evaluate the student's full profile (Education, Field of Study, Country, Financial Need) against active opportunities.
- Ensured the AI returns exactly the top 5 matches containing the opportunityId and a brief matchReason.
- Updated the Student Dashboard (/app/dashboard/page.tsx) to fetch the profile and opportunities, run the Server Action, and beautifully render the matched opportunities as Tailwind Cards featuring the matchReason.
